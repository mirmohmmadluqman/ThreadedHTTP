# How to use via make:
make              # Just build
make run          # Run server
make test         # Run tests  
make quick        # fmt + check + test (daily workflow)
make ci           # Full pipeline before push
make release      # Production build

